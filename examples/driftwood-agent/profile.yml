# driftwood-answer — mounts answer_query over the driftwood wren project (2x2 eval substrate).
profile: driftwood-answer

context:
  project: ./context/binding.yml

config:
  tier_policy: cost_sensitive

components:
  - use: answer_query

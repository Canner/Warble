{
  "_comment": "The known-good MDL a faithful bootstrap_mdl should produce from raw/schema.json. This is the ground truth the schema_fidelity / metric_soundness eval scores a produced MDL against (plan §7.5: a controlled synthetic source + known expected MDL, never a drifting real DB). Shaped as a wren-core-base manifest.",
  "catalog": "wren",
  "schema": "public",
  "models": [
    {
      "name": "orders",
      "tableReference": { "schema": "main", "table": "raw_orders" },
      "columns": [
        { "name": "order_id", "type": "INTEGER" },
        { "name": "customer_id", "type": "INTEGER" },
        { "name": "amount", "type": "DECIMAL" },
        { "name": "status", "type": "VARCHAR" },
        { "name": "ordered_at", "type": "TIMESTAMP" }
      ]
    },
    {
      "name": "customers",
      "tableReference": { "schema": "main", "table": "raw_customers" },
      "columns": [
        { "name": "customer_id", "type": "INTEGER" },
        { "name": "name", "type": "VARCHAR" },
        { "name": "region", "type": "VARCHAR" },
        { "name": "signed_up_at", "type": "TIMESTAMP" }
      ]
    }
  ],
  "relationships": [],
  "cubes": [
    {
      "name": "revenue",
      "baseObject": "orders",
      "measures": [
        { "name": "total_revenue", "expression": "SUM(amount)", "type": "DOUBLE" }
      ],
      "dimensions": [
        { "name": "status", "expression": "status", "type": "VARCHAR" }
      ],
      "timeDimensions": [
        { "name": "ordered_at", "expression": "ordered_at", "type": "TIMESTAMP" }
      ],
      "hierarchies": {}
    }
  ],
  "views": []
}

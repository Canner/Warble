# A pinned, existing semantic project — unlike examples/provision-agent, every component here runs
# only after a layer exists and builds. Path is relative to this Warble project dir.
project: ../jaffle-wren

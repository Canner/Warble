Take stock of the bound project without changing any part of it.

- Read what is already modeled and what raw material is available alongside it. Do not read or expose
  credential values, and do not carry raw excerpts into the output.
- Classify each gap as missing coverage, a disagreement between the raw material and what is
  currently modeled, or an inference. Emit opaque locators, a confidence, and the destination a fix
  would belong in — never the fix itself.
- Do not write files, record state, run validation or a build, or decide anything. A survey that
  quietly repairs what it found has destroyed the evidence that it was ever broken.

Produce `enrichment_gaps` as the input to the drafting step. It is an inventory, not a proposal, and
not authorization to change anything.

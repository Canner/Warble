# Bound wren project, relative to this project-dir. Neither component queries it — the binding only
# satisfies the coarse `wren_project_exists` precondition, keeping the example self-contained (same
# convention as `examples/brief-demo/context/binding.yml`).
project: ../jaffle-wren

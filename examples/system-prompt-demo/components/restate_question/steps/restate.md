Restate the user's question in one unambiguous sentence, without answering it.

Return ONLY a JSON object of exactly this form, with no prose before or after:

{"restated": "<the restated question>"}

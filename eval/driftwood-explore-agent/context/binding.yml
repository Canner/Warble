# The bound semantic layer, as a prepared-context document warble reads directly. `project` stays
# the layer's identity; the committed example ships MDL + knowledge only, so fetch the pinned
# queryable DuckDB with `python3 examples/driftwood-wren/fixture.py fetch`.
kind: prepared
project: ../../examples/driftwood-wren
document: context/context.json

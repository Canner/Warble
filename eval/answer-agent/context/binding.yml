# The bound semantic layer, as a prepared-context document warble reads directly. `project` stays
# the layer's identity (a path into the examples tree); the eval runner injects a queryable project
# (with connection/data) at run time via --project.
kind: prepared
project: ../../examples/jaffle-wren
document: context/context.json

---
title: Components
description: "A component is the reusable behavior unit (a \"data verb\") a profile mounts — a manifest plus optional tool/hook code, carrying a type and a realization_kind."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- The four component types — analytical, assertive, mutating, orchestrating — and how `type` plus `realization_kind` (`skill` / `tool` / `gated-tool`) shape dispatch.
- Component anatomy: `component.yml`, `steps/*.md` prompts, and per-step tiers (`cheap` / `strong`).
- How the flagship `genbi-default` library (`explore_model`, `answer_query`, `generate_dashboard`, `explain_change`) illustrates the pattern.

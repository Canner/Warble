---
title: Render contract
description: "The render contract is the typed-block output contract (kpi_card / table / chart / narrative, …) with two flavors: programmatic (agent emits a JSON blocks envelope, Warble renders HTML deterministically) and prompt (the agent writes the file itself)."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- The stdlib of typed blocks and how they map to a rendered dashboard via `warble render`.
- Programmatic vs. prompt render flavors and the read-only-execution trade-off between them.
- How the `diff` and `status` render blocks extend the stdlib for mutating and assertive components.

---
title: Profiles
description: "Profiles are the git-authoritative declaration of a data agent's behavior: which components it mounts, their config/overrides, guardrails, and the semantic context it binds to."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- Profile anatomy: mounted components, config/guardrail overrides, and context binding, all as diffable YAML.
- How component-manifest defaults merge with profile-level overrides during compilation.
- Why profiles — not prompts baked into an agent loop — are the reviewable, versioned source of truth.

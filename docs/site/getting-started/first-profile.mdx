---
title: Your first profile
description: "A walkthrough of authoring a minimal profile from scratch — mounting a component, binding a semantic context, and compiling it to IR."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- Laying out a profile directory (`profile.yml`, mounted components, context binding) from nothing.
- Choosing which component(s) to mount and which config/guardrail overrides to set.
- Compiling the profile with `warble compile` and reading the emitted IR before dispatching it.

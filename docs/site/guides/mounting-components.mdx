---
title: Mounting components
description: "How to mount one or more components into a profile, override their default config/guardrails, and understand how defaults merge with overrides at compile time."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- The mount syntax in `profile.yml` and which fields are overridable per component.
- Default-and-override merge semantics during compilation.
- Composing multiple components into one profile (e.g. the four `genbi-default` components) without a data-flow DSL.

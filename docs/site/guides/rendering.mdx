---
title: Rendering results into a dashboard
description: "How to turn a captured result envelope into a deterministic HTML dashboard with warble render, and the difference between the programmatic and prompt render flavors."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- Running `warble render result.json --out dashboard.html` against a captured blocks envelope.
- Programmatic flavor (deterministic HTML from typed blocks) versus prompt flavor (the agent writes the file itself).
- Which typed blocks (`kpi_card`, `table`, `chart`, `narrative`, `diff`, `status`) render into which dashboard elements.

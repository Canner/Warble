---
title: Writing a component
description: "How to author a new component: its manifest (type, realization_kind, guardrails, capabilities) plus its steps/*.md prompts and optional tool/hook code."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- Choosing a component `type` (analytical / assertive / mutating / orchestrating) and `realization_kind` (`skill` / `tool` / `gated-tool`).
- Writing per-step prompts and setting per-step tiers (`cheap` / `strong`).
- Declaring the component's guardrails and required capabilities so the dispatcher can resolve them per target.

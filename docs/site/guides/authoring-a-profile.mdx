---
title: Authoring a profile
description: "A practical, step-by-step guide to writing a profile.yml — choosing components to mount, setting config/guardrail overrides, and binding a semantic context."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- A worked `profile.yml` example mounting one or more components with overrides.
- How guardrail overrides interact with `locked` guardrails inherited from a component's manifest.
- Validating and compiling the profile with `warble compile`.

---
title: AI resources
description: "Machine-readable docs for agents working with Warble: a planned llms.txt index, per-page Markdown export, and ready-made prompts for authoring profiles and components."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- An `llms.txt` (and `llms-full.txt`) index of this site, so an agent can fetch the docs as plain Markdown instead of scraping rendered HTML.
- Direct `.md` access for every page (Mintlify's `<page-url>.md` convention), covering the profile schema, IR schema, capability model, and glossary.
- Starter agent prompts for common Warble tasks — authoring a profile, writing a component, and interpreting a `blast_radius` gate result.

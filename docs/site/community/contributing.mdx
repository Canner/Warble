---
title: Contributing
description: "How to contribute to Warble: workspace layout, build/test/lint commands, and the conventions (enum-keyed dispatch, sans-IO core, conventional commits) that keep the design intact."
---

<Note>
This page is planned and not yet written. See the [docs site plan](/community/roadmap) for status.
</Note>

## What this page will cover

- Repo layout (`core/`, `dispatcher/`, `cli/`, `genbi-default/`, `examples/`, `docs/spec/`) and the `just` recipes for build/test/lint.
- The invariants new contributions must preserve (enum-keyed dispatchers, sans-IO/zero-wren core, no DSL in composition, runtime-agnostic IR).
- Commit and PR conventions (conventional commits: `feat` / `fix` / `chore` / `refactor` / `test` / `docs`).

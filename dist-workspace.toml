[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# release.yml has its third-party actions manually pinned to commit SHAs (matching
# ci.yml's convention) after generation; that pinning makes the file differ from what
# `dist generate` would emit natively (which uses floating tags), so tell dist not to
# treat that as drift.
allow-dirty = ["ci"]
# The installers to generate for each app
installers = ["shell", "npm"]
# Scope to prefix npm packages with (required by the npm installer)
npm-scope = "@warble"
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Where to host releases
hosting = "github"
# Whether to install an updater program
install-updater = false

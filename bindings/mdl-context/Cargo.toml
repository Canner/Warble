[package]
name = "warble-mdl-context"
version.workspace = true
edition.workspace = true
description = "Warble ContextLoader adapter #1 — introspects a wren MDL project (via wren-core-base) into Warble's Info projections + semantic lineage. Binding-layer only; keeps core zero-wren."

[dependencies]
warble.workspace = true
wren-core-base.workspace = true
serde = { workspace = true }
serde_yaml.workspace = true
serde_json.workspace = true
sqlparser.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true

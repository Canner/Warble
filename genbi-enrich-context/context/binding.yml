# A pinned, existing semantic project. Runtime hosts replace this fixture binding with the project
# selected at bind time and must present a successful context-build proof before starting a run.
project: ../examples/jaffle-wren
